name: 'Microsoft Store App Publisher'
description: 'Setup Microsoft Store Developer CLI'
author: 'Microsoft'
branding:
  icon: 'upload-cloud'
  color: 'blue'
inputs:
  version:
    required: false
    description: "The version of the Microsoft Store CLI to be downloaded and configured (e.g. 'v0.3.0')."
    default: 'latest'
runs:
  using: 'node24'
  main: 'dist/index.js'
